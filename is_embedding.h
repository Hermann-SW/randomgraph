
/*
 *  generates the topological embedding D dual to the topological embedding
 *  in the planar graph H. It returns true on success and false if G is not
 *  a topological embedding.
 */
bool is_embedding(void);

num CONNECTED_COMPONENTS(vertex_array compnum);

